apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-deployment
  namespace: community
spec:
  replicas: 1
  selector:
    matchLabels:
      app: post-service
  template:
    metadata:
      labels:
        app: post-service
    spec:
      containers:
        - name: post-service
          image: nosmokingarea/post-service:latest
          ports:
            - containerPort: 8000

---
apiVersion: v1
kind: Service
metadata:
  name: post-service
  namespace: community
spec:
  selector:
    app: post-service
  type: NodePort
  ports:
    - protocol: TCP
      port: 8000        # Cluster 내부 포트
      targetPort: 8000  # Pod 컨테이너 포트
      nodePort: 32093   # 외부 접속용 포트 (30000~32767)
